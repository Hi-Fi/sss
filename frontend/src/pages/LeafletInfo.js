import React, { Component } from 'react';
import LeafletInfoContainer from '../containers/LeafletInfoContainer';

class LeafletInfo extends Component {
  render() {
    return (
      <div className='container'>
        <LeafletInfoContainer/>
      </div>
    );
  }
}

export default LeafletInfo;